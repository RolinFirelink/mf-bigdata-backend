<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arg.smart.web.statistics.mapper.MarketConcentrationMapper">

    <update id="updateMarketConcentration">
        UPDATE sh_market_concentration AS m
        SET m.market_concentration = (
            SELECT SUM(s.sales)
            FROM sh_company_sales AS s
            ORDER BY s.sales DESC
            LIMIT 4
        );

    </update>
</mapper>
